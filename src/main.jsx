import React from "react";
import ReactDOM from "react-dom/client";
import TradeSimulatorApp from "./TradeSimulatorApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TradeSimulatorApp />
  </React.StrictMode>
);
